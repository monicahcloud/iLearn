export interface Assignments {
    id?: number ;
    assignId?: number,
    name?: string,
    des?: string,
    grade?: string,
    createDate?: Date,
    subjectId?: number,
    userId?: number,
    content?: string,
}